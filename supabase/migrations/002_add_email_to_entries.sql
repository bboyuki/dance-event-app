-- ============================================================
-- Migration 002: entries テーブルに email カラムを追加
-- ============================================================
-- 適用手順:
--   Supabase ダッシュボード > SQL Editor に貼り付けて実行
-- ============================================================

ALTER TABLE entries
  ADD COLUMN IF NOT EXISTS email TEXT NOT NULL DEFAULT '';
